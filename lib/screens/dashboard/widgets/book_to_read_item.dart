import 'package:flutter/material.dart';

class BookToReadItem extends StatelessWidget {
  const BookToReadItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
